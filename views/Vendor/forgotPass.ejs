<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>DashDrive Vendor - Forgot Password</title>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;500;600&display=swap" rel="stylesheet">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Poppins', sans-serif;
        }

        body {
            background-color: #faf9f6;
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
        }

        .container {
            width: 100%;
            max-width: 400px;
            padding: 2rem;
            margin: 0 1rem;
        }

        .form-card {
            background: white;
            padding: 2rem;
            border-radius: 10px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        }

        .logo-container {
            text-align: center;
            margin-bottom: 2rem;
        }

        .logo-container img {
            max-width: 150px;
            height: auto;
        }

        h1 {
            color: #2d2d2d;
            font-size: 1.75rem;
            text-align: center;
            margin-bottom: 1.5rem;
        }

        .form-group {
            margin-bottom: 1.5rem;
        }

        label {
            display: block;
            color: #2d2d2d;
            margin-bottom: 0.5rem;
            font-weight: 500;
        }

        input {
            width: 100%;
            padding: 0.75rem;
            border: 1px solid #ddd;
            border-radius: 5px;
            font-size: 1rem;
            transition: border-color 0.3s;
        }

        input:focus {
            outline: none;
            border-color: #333333;
        }

        .submit-btn {
            width: 100%;
            padding: 0.75rem;
            background: #333333;
            color: white;
            border: none;
            border-radius: 5px;
            font-size: 1rem;
            font-weight: 500;
            cursor: pointer;
            transition: background-color 0.3s;
        }

        .submit-btn:hover {
            background: #1a1a1a;
        }

        .back-link {
            display: block;
            text-align: center;
            margin-top: 1.5rem;
            color: #333333;
            text-decoration: none;
            font-weight: 500;
        }

        .back-link:hover {
            color: #1a1a1a;
        }

        .message {
            text-align: center;
            margin-bottom: 1.5rem;
            font-size: 0.9rem;
            color: #666;
        }

        .error-message {
            text-align: center;
            margin-bottom: 1.5rem;
            font-size: 0.9rem;
            color: #dc3545;
            /* Red for errors */
        }

        @media (max-width: 480px) {
            .container {
                padding: 1rem;
            }

            .form-card {
                padding: 1.5rem;
            }

            h1 {
                font-size: 1.5rem;
            }
        }
    </style>
</head>

<body>
    <div class="container">
        <div class="form-card">
            <div class="logo-container">
                <img src="/images/logoDashDrive.png" alt="DashDrive Logo">
            </div>
            <h1>Reset Password</h1>
            <p class="message">Enter your email address and we'll send you instructions to reset your password.</p>
            <% if (typeof message !== 'undefined' && message) { %>
                <p class="error-message"><%= message %></p>
            <% } %>
            <form action="/vendor/forgot-password" method="POST">
                <div class="form-group">
                    <label for="email">Email Address</label>
                    <input type="email" id="email" name="email" required placeholder="Enter your email">
                </div>

                <button type="submit" class="submit-btn">Send OTP</button>
            </form>

            <a href="/vendor/login" class="back-link">Back to Login</a>
        </div>
    </div>
</body>

</html>